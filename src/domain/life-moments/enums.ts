export enum PathwayStatus {
  active = 'active',
  closed = 'closed',
  inactive = 'inactive',
}

export enum PathwaySubscriptionStatus {
  subscribed = 'subscribed',
  not_subscribed = 'not_subscribed',
}
